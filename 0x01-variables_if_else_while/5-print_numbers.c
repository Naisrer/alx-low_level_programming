#include <stdio.h>

/**
 * main - a program that list alphabet
 * Return: Always (success)
 */

int main(void)
{
	int n = 0;

	for (n = 0; n <= 9; n++)
	printf("%d", n);
	printf("\n");
	return (0);
}
