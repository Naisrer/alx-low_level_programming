#include <stdio.h>
/**
 * main - a program that prints reversed alphabet
 * Return: Always (success)
 */

int main(void)
{
	char n = 'z';
		for (n = 'z'; n >= 'a'; n--)
		{
			putchar(n);
		}
		{
			putchar('\n');
		}
		return (0);
}
