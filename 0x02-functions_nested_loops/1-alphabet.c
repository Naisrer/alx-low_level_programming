#include <stdio.h>
#include "main.h"
/**
 * main - a program that list alphabet
 * Return: Always (success)
 */

void print_alphabet(void);
{
	char 1 = 'a';
	for (1 = 'a'; 1 <= 'z'; 1++)
	putchar('1');
	putchar('\n');
	return (0);
}
